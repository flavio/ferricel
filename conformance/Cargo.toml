[package]
edition = "2024"
name    = "conformance"
version = "0.1.0"

[dependencies]
ferricel-core  = { path = "../ferricel-core" }
ferricel-types = { path = "../ferricel-types" }
serde_json     = { workspace = true }
slog           = { workspace = true }
slog-term      = "2.9"

# Protobuf dependencies for conformance test parsing
base64      = "0.22"
prost       = "0.14"
prost-types = "0.14"
serde       = { workspace = true }

# Parallel execution
rayon = "1.10"

[build-dependencies]
prost-build = "0.14"
